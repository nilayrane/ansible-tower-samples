- name: Hello World Sample
  hosts: all
  vars: 
    arcon_cred:
      username: '{{ lookup("env","username") }}'
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!{{ username }}"

