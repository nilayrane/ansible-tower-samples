- name: Hello World Sample
  hosts: all
  vars: 
    arcon_cred:
      username: '{{ lookup("credentials","test2") }}'
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!{{ arcon_cred }}"

