- name: Hello World Sample
  hosts: all
  vars: 
    arcon_cred:
      username: '{{ lookup("env","user") }}'
  tasks:
    - name: Hello Message
      debug:
        msg: "Hello World!{{ username }}"

